// 객체란?
// 객체는 연관 배열 => 몇 개의 값을 모은 데이터 이다.
const objFooBar = {
  foo: 123,
  bar: "Hello, world!",
};
console.log(objFooBar);
console.log(objFooBar.foo);
console.log(objFooBar.bar);

// 객체 리터럴
// 콤마로 구분된 프로퍼티의 정의가 나열
// 프로퍼티 => 객체 안의 값 하나하나를 가리키는 명칭

// 프로퍼티 접근 => 객체 내용(프로퍼티)의 값을 얻는 것
